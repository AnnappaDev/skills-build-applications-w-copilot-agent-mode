# OctoFit Tracker

This folder contains the OctoFit Tracker application, with separate `backend/` and `frontend/` subfolders.

Follow the project setup instructions in `.github/instructions/` for details on environment and dependencies.
